
/*
 * checks all values of gamestate to check if they are correctly initialized.
 * This only checks for wrong or missing values. Nothing positional!
 *
 * @param gamestate: pointer to gamestate to check 
 *
 */*
void gamestateCorrectlyLoadedCheck(Gamestate* gamestate) {
    return;
}

/**
 * initializes all variables with values and returns an initialized gamestate.
 * This should be the only way to get a new gamestate.
 */
Gamestate* gamestateInit() {
    return;
}
